name: Deploy PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  build-and-deploy-preview:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install Firebase CLI
      run: npm install -g firebase-tools
      
    - name: Install Flutter dependencies
      run: |
        cd app
        flutter pub get
        
    - name: Run Flutter tests with coverage
      run: |
        cd app
        flutter test --coverage
        
    - name: Generate coverage report
      run: |
        cd app
        # Install lcov for coverage report generation
        sudo apt-get update
        sudo apt-get install -y lcov
        # Generate HTML coverage report
        lcov --capture --directory coverage --output-file coverage/lcov.info
        genhtml coverage/lcov.info --output-directory coverage/html
        
    - name: Build Flutter web app
      run: |
        cd app
        flutter build web --release
        
    - name: Deploy PR preview to Firebase Hosting
      run: |
        cd app
        # Create a unique site name for this PR
        PR_SITE_NAME="minecraft-server-admin-pr-${{ github.event.pull_request.number }}"
        
        # Create a temporary Firebase project for this PR
        firebase projects:create $PR_SITE_NAME --display-name "Minecraft Server Admin PR #${{ github.event.pull_request.number }}" --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Initialize hosting for the temporary project
        firebase init hosting --project $PR_SITE_NAME --public build/web --yes --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Deploy to the temporary site
        firebase deploy --only hosting --project $PR_SITE_NAME --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Output the preview URL
        echo "PR_PREVIEW_URL=https://$PR_SITE_NAME.web.app" >> $GITHUB_ENV
        echo "PR_PREVIEW_URL=https://$PR_SITE_NAME.web.app" >> $GITHUB_OUTPUT
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        
    - name: Deploy coverage report
      run: |
        cd app
        # Create a unique site name for coverage report
        COVERAGE_SITE_NAME="minecraft-server-admin-coverage-pr-${{ github.event.pull_request.number }}"
        
        # Create a temporary Firebase project for coverage
        firebase projects:create $COVERAGE_SITE_NAME --display-name "Minecraft Server Admin Coverage PR #${{ github.event.pull_request.number }}" --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Initialize hosting for coverage
        firebase init hosting --project $COVERAGE_SITE_NAME --public coverage/html --yes --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Deploy coverage report
        firebase deploy --only hosting --project $COVERAGE_SITE_NAME --token ${{ secrets.FIREBASE_TOKEN }}
        
        # Output the coverage URL
        echo "COVERAGE_URL=https://$COVERAGE_SITE_NAME.web.app" >> $GITHUB_ENV
        echo "COVERAGE_URL=https://$COVERAGE_SITE_NAME.web.app" >> $GITHUB_OUTPUT
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        
    - name: Comment PR with preview links
      uses: actions/github-script@v7
      with:
        script: |
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
          });
          
          const botComment = comments.find(comment => 
            comment.user.type === 'Bot' && 
            comment.body.includes('🚀 Preview deployed')
          );
          
          const previewUrl = '${{ steps.build-and-deploy-preview.outputs.PR_PREVIEW_URL }}';
          const coverageUrl = '${{ steps.build-and-deploy-preview.outputs.COVERAGE_URL }}';
          
          const commentBody = `## 🚀 Preview deployed!
          
          **App Preview:** ${previewUrl}
          **Coverage Report:** ${coverageUrl}
          
          This preview will be automatically updated when you push new commits to this PR.
          
          ---
          *This comment will be updated automatically.*`;
          
          if (botComment) {
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: botComment.id,
              body: commentBody
            });
          } else {
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });
          }
          
    - name: Upload coverage reports
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report-pr-${{ github.event.pull_request.number }}
        path: app/coverage/
